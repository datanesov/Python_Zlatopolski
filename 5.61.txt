x, y = map(int, input().split()) # ввод чисел x и y
product = 0 # инициализация произведения

while y != 0: # пока y не станет нулем
    if y % 2 == 1: # если y нечётное
        product += x # добавляем x к произведению
    x = x * 2 # удваиваем x
    y //= 2 # уменьшаем y вдвое

print(product)
