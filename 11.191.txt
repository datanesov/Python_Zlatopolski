a)

arr = [1, 0, 5, 0, 8, 9]
flag = False  # флаг для определения первого нулевого элемента
for ele in arr:
    if flag:  # если первый нулевой элемент уже найден
        print(ele)
    elif ele == 0:  # если текущий элемент нулевой и первый еще не найден
        flag = True

б)
arr = [1, 0, 5, 0, 8, 9]
flag = False  # флаг для определения последнего нулевого элемента
for ele in arr[::-1]:  # проходим по массиву в обратном порядке
    if flag:  # если последний нулевой элемент уже найден
        print(ele)
    elif ele == 0:  # если текущий элемент нулевой и последний еще не найден
        flag = True
