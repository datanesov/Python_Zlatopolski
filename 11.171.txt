max_size = n + sum(1 for i in arr if i < 0) # максимальный размер исходного массива

new_arr = [0] * max_size # создаем новый массив с максимальным размером

j = 0 # индекс для нового массива

for i in range(n): # проходим по исходному массиву
    new_arr[j] = arr[i] # вставляем текущий элемент из исходного массива
    j += 1
    if arr[i] < 0: # если текущий элемент отрицательный
        new_arr[j] = p # вставляем число p
        j += 1

if j < max_size: # если массив был заполнен не до конца
    new_arr[j] = p # добавляем число p в конец нового массива

print(new_arr)
