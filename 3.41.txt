n = int(input("Введите число n (1 <= n <= 999): "))

# находим последнюю цифру числа х
last_digit = n % 10

# переставляем цифры в оставшемся двузначном числе и приписываем последнюю цифру
new_num = (n // 10) % 10 * 100 + (n // 100) * 10 + last_digit

print("Число х:", new_num)
