# Решение задачи методом индексации:

# а) а_1 + a_n
n = int(input('Введите количество чисел: '))
a = [int(input(f'Введите число a_{i+1}: ')) for i in range(n)]
print(f'a_1 + a_n = {a[0] + a[n-1]}')

# б) a_2 + a_n-1
print(f'a_2 + a_n-1 = {a[1] + a[n-2]}')
